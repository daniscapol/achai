# Production Environment Configuration
# This is a static frontend-only deployment
# No API server needed - works entirely with static MCP data

NODE_ENV=production
# Leave VITE_API_BASE_URL empty to disable API calls in production
VITE_API_BASE_URL=